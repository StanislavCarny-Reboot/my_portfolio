## Insights 
1. prompts
    - user controlled never be called by the llm 
2. resources 
    - user controlled never be called by the llm 
3. tools
    - called by llms 


## Instructions y

1. run mcp server
` uv run python server.py `
2. run client 
` ./ client.py `
 
 
 
 ## Other commands
 - install tools to cursor 
 `uv run fastmcp install cursor server.py `

 - inspect mcp in the browser
 `uv run fastmcp dev server.py `


